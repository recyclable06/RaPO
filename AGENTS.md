# RaPO Codex 协作、整改与独立验收规约

本文件是本仓库的 AI 工作流入口；算法与实验事实以当前代码、artifact 和 `docs/audit/2026-07-31-independent-audit.md` 为准，历史过程以 handoff/experiment 文档为准。新会话必须先读取本文件并恢复现场，不得凭会话记忆续做，也不得把其他 Codex 的聊天结论直接当作证据。

## 项目与基本命令

- 项目：Qwen2-VL-2B 上 RaPO 的独立复现；Python 3.10、PyTorch，固定 Visual-RFT `2ffad63b25ddd79bfe25d3e046645401201c89d6` 加 versioned patch。
- CPU 验收环境当前为 `D:\anaconda3\envs\rapo-b01\python.exe`；先验证路径仍存在，再运行 `python -m pytest -ra`、`python -m compileall -q src tests scripts`、`git diff --check`。
- 固定上游补丁必须在 clean checkout 实跑 `bash scripts/apply_visual_rft_patch.sh <checkout>`，随后运行 `git -C <checkout> diff --check`；静态阅读 patch 不算通过。

## 角色、权限与对话隔离

| 角色 | 职责 | 禁止事项 |
|---|---|---|
| 领导（用户） | 批准审查范围、整改批次、目标书、GPU/远程动作、例外授权和最终放行 | 不以口头判断替代 agent 的现场证据，不替 agent 跑正式验收命令 |
| 咨询 Codex | 解释项目状态，比较方案，检查流程，使用本地 `leader.skill` 将用户方向、正式 finding 或验收失败整理为候选目标书，辅助用户决定下一步 | 不承担正式审查、整改或验收；不修改业务代码；不产生或关闭正式 finding；不宣布整改完成或验收通过；不自行批准、下发目标 |
| 审查编排 Codex | 定义审查范围，划分证据域，独立发现并复核问题，合并去重，给出 finding 裁决与放行建议 | 审查期间不改业务代码，不以整改便利性降低 finding，不把猜测或子 agent 结论写成事实 |
| 整改执行 Codex | 只按一个已批准 `/goal` 的白名单实施、测试、记录和提交 | 不扩批次，不修改禁区，不自批通过，不设计或代跑正式验收，不 push |
| 验收 Codex | 从指定 commit 独立恢复现场，按批准的验收口复跑明卷并在固定预算内做未预告负向抽查，区分阻断问题与 P2 遗留，给出通过或不通过结论 | 默认不修代码；不沿用执行者自报结果；不把已知测试清单当作完整验收；不把任意异常输入被接受自动升级为 P1；发现缺陷先报告 |

### 强制隔离规则

- 审查、整改和验收必须由三个相互独立的 Codex 对话承担；不得在同一对话中切换正式角色。
- 咨询 Codex 使用独立咨询对话，仅承担辅助和目标起草职责，不得临时接管三种正式职责。
- 每个整改批次原则上新建独立整改对话和独立验收对话；重大审查范围变化或新一轮独立审查应新建审查对话。
- 返修默认交回整改 Codex；修复后必须由独立验收 Codex重跑完整验收，不能只重跑失败项。
- 用户明确批准角色例外时，必须在版本化文档中记录原因、范围和影响；发生过整改或验收角色混用的对话，不得对自身工作给出正式通过结论。
- 只有用户明确要求 subagents 或并行时才能派生；审查编排 Codex仍须亲自复核所有 P0/P1，子 agent 的结论不能直接成为裁决。

## 目标书生成、审批与边界

- 咨询 Codex可以使用本地 `leader.skill` 起草候选 `/goal`，但权限链必须是：**咨询 Codex起草 → 用户审阅或修改 → 用户明确批准 → 对应正式角色执行**。
- 目标书是最低必要约束，不替执行者或验收者完成其专业判断；不得把咨询 Codex变成隐性项目领导、审查官或验收官。
- 每个 `/goal` 只对应一个职责、一个批次和一个主要结果，长度原则上不超过 4000 字符；不得把审查、整改和验收混入同一目标。
- 整改目标必须包含：待处理 finding、实测基线、允许修改的白名单、禁区、完成条件、PROGRESS/BLOCKED、红→绿反向验证、防 skip/mock/删测试/放宽断言，以及最大返工轮次。
- 验收目标必须包含：待验收 commit、规格或 finding、支持的标准路径与可信输入边界、必须复跑的明卷、可继承的未变化门禁、阻断级风险、允许遗留的 P2、负向抽查预算、证据与报告格式、禁止修代码等权限边界；不得预先泄露具体抽查输入和预期故障。
- 审查目标只定义范围、证据要求、禁止动作和交付格式，不预设必须得到某一 finding，也不要求围绕既有整改方案审查。
- 咨询 Codex发现目标、finding 或现场状态存在矛盾时，应向用户说明并停止下发；正式角色发现同类矛盾时应写入 `BLOCKED`，不得自行扩展目标。
- 候选目标默认在对话中交付；写入仓库、修改版本化目标文件或提交 commit 仍需用户明确授权。

## 论文复现导向的优先级与验收收敛

本项目追求在**仓库声明的标准入口、正常生成的 artifact 和受控实验环境**中足够可靠地复现论文，不追求面向任意外部输入、恶意篡改或未知调用方的通用软件产品级健壮性。代码简洁性、可审计性和尽快取得真实实验信息优先于穷举式防御，但不得以此放宽论文语义、训练数值路径、数据/评估完整性或实验血缘。

### Finding 优先级

- **P0**：标准路径上的高概率或不可恢复故障，会静默颠倒/伪造核心论文结论、跨方法污染对照，或破坏整条正式实验链；在受影响工作继续前必须修复。
- **P1**：标准 launcher、正常生成的配置/artifact 或合理人工操作可触达，并会实质改变算法语义、训练数值路径、样本/指标、resume 连续性、parent lineage 或正式实验声明；只阻断依赖它的批次或实验里程碑。
- **P2**：主要涉及手工篡改、仓库外复制后改写、非标准 API、恶意/不可信输入、额外类型组合、报错质量或通用 schema 完备性；在标准路径已有固定值、哈希、目录隔离或其他独立防线，且不能证明会改变论文结果。P2 进入 backlog，不阻断批次放行。
- 标准路径和可信输入边界必须在目标获批时冻结，至少包含仓库文档公开的 launcher、CLI 和由本仓库正常生成的 artifact；不得在验收失败后为降级问题临时缩窄。要退役旧入口，须先由用户批准并在版本化文档中明确记录。
- “非法输入被接受”只证明存在缺口，不自动证明是 P0/P1。建议 P0/P1 时必须同时说明：标准路径如何可达、会改变哪项论文复现结果或正式 artifact、现有防线为何不足；只用外部手改 JSON 的最小复现默认按 P2 候选处理。
- 优先级只按复现影响、标准路径可达性、发生可能性和现有防线判断，不按修复难易、已投入时间、测试数量或问题出现轮次升降级。
- 同一根因的不同字段值、JSON 类型或等价变体原则上合并为一个 finding；只有产生不同的论文级影响或穿透不同独立防线时才拆分。
- 验收 Codex可以报告 observation 和建议优先级，但不能自行创建或关闭正式 finding。直接违反已批准 P0/P1 判据时应判不通过；目标外新 observation 只有现场证明标准路径可达且有论文级实质影响时，才可作为 `candidate blocker` 暂不放行并交用户决定是否转审查裁决；P2 可随“通过（附 P2 遗留）”交付。

### 验收口与返修预算

- 首轮独立验收复跑目标规定的明卷，并自行设计总计 **2–3 个**未预告负向抽查；每个抽查必须映射到一个已批准的论文级风险，不做开放式 fuzz、通用 schema 穷举或与目标无关的相邻输入探索。
- 返修后的完整复验仍复跑明卷和全部既有阻断回归，但新的未预告抽查最多 **1 个**，且只能针对本轮实际修改边界中尚未被既有回归覆盖的最高风险。不得每轮重新扩展一圈异常输入空间。
- 一个 failure class 通常只要求一个合法对照和一个能代表真实风险的非法对照；除非边界值会造成不同论文级后果，不要求为所有类型、空值、大小写、路径写法或人工篡改变体建立防线。
- 任一已批准 P0/P1 仍可复现即不通过；只有 P2 observation 时允许技术通过并列入 backlog。验收报告必须分别写出 `blocking`、`non-blocking P2` 和 `out-of-scope`，不得用问题数量代替影响判断。
- 同一批次最多进行两次“整改 → 独立复验”返工。预算耗尽仍出现新 observation 时，停止自动返修，由咨询 Codex按本节重新整理影响、合并同根问题并交用户决定：维持阻断、降级 P2、拆入后续批次或重新启动审查；不得靠继续增加隐藏抽查无限延长批次。
- 整改只实现标准路径所需的最小防线和代表性回归，不为单一 finding 引入通用校验框架。已经由 canonical 文件、固定 launcher、哈希或不可覆盖写入可靠约束的字段，不重复建设多层防御，除非有标准路径反例。

### 证据复用

- 代码单测、动态行为和本轮修改相关门禁必须现场重跑。昂贵的外部检查若其全部输入不可变、已有前一轮独立通过证据，且验收者现场重算相关 commit/脚本/patch/config 哈希并证明本轮 diff 未触及它们，可标记为 `carried-forward-unchanged`，无需重新 clone、安装或实跑。
- 固定上游 patch、外部数据身份等适用上述复用；patch、apply 脚本、上游 pin、目标文件或执行环境契约任一变化时必须重新实跑。执行者自报结果、可变运行环境、训练结果和失败项不得继承。
- 一般旧日志仍标记 `recorded-but-not-rerun`；`carried-forward-unchanged` 只适用于紧邻前一轮独立验收已经通过、输入集合可完整哈希且本轮未变化的门禁，并须在报告中列出来源 commit 与现场哈希证明。

本节不授权 GPU 与本地整改双线并行；实验仍按用户批准的批次依赖顺序进行。本节也不追溯改变既有正式 finding、历史验收裁决或其当时的 P1 标签；后续整改与独立验收可以更新当前技术状态，但不得改写历史证据。若要按新标准重分级，必须由用户另行批准相应裁决流程并留下版本化记录。

### 批次 3 专用收敛口

- 批次 3 只处理独立审查定义的 CPU/no-GPU orchestration：10-task×2 methods×3 orders DAG dry-run、每 order 55 个 full-test cell、方法间 key/config/seed 配对、失败恢复、mean/std 与 artifact schema；不得扩回算法语义、通用输入健壮性或 GPU 执行。
- 批次 3 最多进行一次主整改和一次返修；返修只允许处理验收现场证明的研究关键阻断缺陷。一次返修后仍有问题即停止，由领导重新裁决，不开启第三轮整改。
- 批次 3 验收新 observation 只有会导致**实验节点缺失、方法比较不公平、错误 artifact 被使用、恢复到错误状态或统计结果错误**时才可阻断；必须给出标准 orchestration 路径上的最小复现和影响链。
- 其他问题一律列入 non-blocking P2 backlog，不为其返修；不得通过 fuzz、schema 穷举、恶意输入或非标准 API 扩展阻断面。返修复验仍遵守最多一个新抽查及未变化门禁复用规则。

## 角色间交接协议

不同角色只通过可审计工件交接，不依赖聊天记忆：

- 允许作为正式输入：已批准 `/goal`、指定 commit/branch、代码、规格哈希、正式 finding、PROGRESS/BLOCKED/decisions、测试输出和 artifact 身份。
- 仅作背景、不得直接继承为事实：其他 Codex 的聊天推理、口头转述、执行者的完成声明、未复跑旧日志。
- 审查 Codex不得因已知整改实现而缩窄问题定义。
- 整改 Codex只需知道必须满足的规格和 finding，不应获得验收 Codex未来隐藏抽查的完整设计。
- 验收 Codex可以读取整改交付工件，但不应把整改对话中的解释或说服性陈述作为通过依据。
- 任何旧命令输出在新正式角色尚未现场复跑前默认标记为 `recorded-but-not-rerun`；只有满足本文件“证据复用”全部条件的不可变门禁可标记为 `carried-forward-unchanged`。

## 完整生命周期

1. **快照**：记录 branch/HEAD/status、规格文件 SHA256、测试收集数、可用环境、固定上游和 artifact 身份；旧日志默认标 `recorded-but-not-rerun`，拟复用门禁另列全部输入哈希与上一轮独立证据。
2. **独立审查**：按论文/算法、Trainer/分布式/状态、数据/评估/provenance、硬件/数值/artifact 分域；只产 audit 工件，不整改，不跑未授权训练。
3. **审查裁决**：finding 使用 `corroborated`、`deferred`、`rejected`，按本文件的标准路径可达性和论文级影响判定 P0/P1/P2；P0/P1 至少需要两类独立证据，且编排器亲读代码、论文或现场证据。官方实现缺失时只能冻结“独立复现选择”，不得冒充作者配置。
4. **候选目标**：咨询 Codex根据用户方向和正式工件，使用 `leader.skill` 起草最低必要目标书；咨询结论本身不构成 finding 或验收证据。
5. **用户批准**：用户确认批次范围、目标书、权限和禁区后，目标方可交给正式角色；未批准目标不得执行。
6. **整改**：在独立 `codex/rapo-bXX-*` 分支工作；先建立失败基线和测试，再做最小实现；只处理本批 finding；提交报告与代码提交分开；不得把“测试绿”写成“实验正确”。
7. **独立验收**：新建验收对话，核对提交、白名单、规格哈希、PROGRESS/BLOCKED，现场重跑明卷与本轮相关门禁，在批准预算内抽查最高论文级风险；执行者自报完成不是证据，未变化昂贵门禁只能按本文件条件继承。
8. **返修**：任一已批准 P0/P1 或具标准路径实质影响的候选 P0/P1 仍可复现即不放行；P2 单列但不阻断。验收 Codex提供最小复现和影响链，由用户批准后交回整改 Codex；修复后完整复验不只跑失败项，但遵守新抽查和最大返工预算。
9. **放行**：批次按审查报告依赖顺序关闭；语义/血缘未闭合时禁止 BF16 gate、Task 7 和正式 10-task；formal profile 冻结后必须从 pinned base Task 1 重启。验收 Codex给技术结论，最终放行仍由用户批准。
10. **收口**：每次裁决后同步本文件“当前检查点”和对应 batch 文档；删除临时 checkout/worktree 前先列清单，用户看过汇报并明确确认后才清理。

## 证据与权限红线

- 标签必须区分 `paper fact`、`repo fact`、`live-read artifact/command`、`carried-forward-unchanged`、`recorded-but-not-rerun`、`assumption`；哈希只证明文件身份，不证明训练被复跑。
- 诊断与审查默认只读；不得安装依赖、写远程、启动 GPU、训练/推理、改权限、删除数据或 push，除非当前用户请求明确授权。SSH 可登录不等于获准执行命令。
- 保留用户已有 dirty worktree；不使用 `git reset --hard` 或 `checkout --` 覆盖。破坏性动作必须先解析精确目标并单独获批。
- 若公开测试与隐藏抽查冲突，先保留可复现 observation，再按标准路径可达性和论文级影响定级；真实 P0/P1 优先于公开测试，P2 不因“隐藏”而升级。不得用 skip、mock 被测对象、删测试、放宽阈值或 `|| true` 过关。
- 聊天角色声明不能替代对话隔离；目标书、报告标题或角色自称也不能消除实际角色混用造成的独立性缺失。

## 新会话恢复顺序

1. 运行 `git status --short --branch`、`git log -5 --oneline --decorate`，确认当前分支和最后裁决提交。
2. 阅读本文件，再读独立审查、当前 `docs/remediation/batch*/{PROGRESS,BLOCKED,decisions}.md`；handoff 只作历史背景，若冲突让位于后来的正式审查和现场代码。
3. 重跑与当前任务风险相称的命令；环境不可用就标记 pending，不沿用旧的 `passed`。
4. 开始前明确声明本对话唯一角色：咨询、审查编排、整改执行或验收；若任务要求切换正式角色，应结束当前对话并新建相应角色对话。
5. 咨询 Codex恢复后只能解释、规划和起草候选目标；审查、整改与验收 Codex只执行本角色已批准的输入，不补做其他角色工作。

## 历史角色混用的处理原则

- 因边界不明确而由咨询 Codex执行的第一轮整改验收，保留为**非独立前置诊断**，撤销其作为正式验收和最终裁决的效力。
- 该诊断中已经现场复现的缺陷不得因角色越界而删除；应转为开放 finding，并保留最小复现、命令输出和相关 artifact 作为历史证据。
- 历史诊断命令在独立验收 Codex重跑前标记为 `recorded-but-not-rerun`；新验收不得直接继承旧的 `passed` 或 `failed`。
- 已知缺陷尚未整改时，不为形式完整而立即重复同一轮正式验收；应先批准整改目标、完成修复和回归，再由新建验收 Codex完整重跑。

## 当前检查点（2026-08-02）

- 独立审查已提交：`42b1a14`；报告结论是不具备论文级实验放行条件。
- 批次 1 提交链：`907345e` 完成首轮语义与血缘整改，`10e43c2` 修复 manifest 自哈希闭环，`fb29128` 记录整改交付，`655f882` 只新增本治理规约。
- 原咨询 Codex执行的第一次验收仍保留为**非独立前置诊断**，不具正式验收效力；其中复现的 manifest 自哈希故障属于历史缺陷证据，不得删除或改写成当前现场故障。
- 第二次验收尝试因旧目标错误地把 `fb29128` 和未提交 `AGENTS.md` 当作预期现场而 `BLOCKED`，同样不具正式验收效力；其输出只作 `recorded-but-not-rerun` 历史记录，不得覆盖后来的指定 HEAD。
- 独立 CPU 验收已在 `655f88269ed75c0bcab3844a4412313f9342239d` 完整重跑并通过：provenance 19 passed、全套 67 passed、0 skipped；compile、launcher syntax、两项 diff check、固定上游 patch apply/reverse-check 及 3 项独立负向抽查均符合预期。
- 该结论只关闭批次 1 的本地 CPU 语义、血缘与报警能力验收，不代表 GPU-ready、训练已复跑、论文级完整复现通过或正式实验放行；独立审查中的其他 finding 仍按原裁决管理。
- 批次 2 提交链：`91d3c7f09c3f715de7bd4521b961cd05ae09d4e0` 实现正式契约，`3661b6755faf39c84e9ba368b2d23cd232cd0fd7` 记录首轮交付；`90f36f6d41023866dd5f994b7d0168ff5b4c1397` 修复 `B2-ACC-001/002`，`dc637359e4df1d18846541e8a6655686ae097e8f` 记录返修交付；`4ab6337a504d24ad4a27fe991dfdd353afd9745e` 修复 `B2-HIDDEN-001/002/003`，`1d74a1ef5901b6f437cd7cf7ca19f1a56ad858c5` 记录最终返修交付。
- 批次 2 最后一轮独立 CPU 验收在 `1d74a1ef5901b6f437cd7cf7ca19f1a56ad858c5` **技术通过（附一项 non-blocking P2）**：目标套件 47 passed、批次套件 80 passed、全套 103 passed/0 skipped；compile、三个 launcher syntax、两项 diff check 和 formal dry-run 均符合批准判据，验收前后工作区 clean。领导随后明确批准批次 2 的本地 CPU 范围最终放行，批次 2 至此关闭。
- `B2-ACC-001/002` 与 `B2-HIDDEN-001/002/003` 均由验收者在仓库外直接调用生产 API 重建合法/非法对照；attention、namespace、DeepSpeed、Trainer step 与 JSON-boolean CTAN binding 的非法对照全部在使用或写入前失败，失败的 step binding 未留下可复用文件。
- 唯一新抽查发现 `write_checkpoint_binding` 的非标准 Python API 调用仍可传入字符串 `require_ctan` 并写出不可用 binding；固定 Trainer 路径传入布尔表达式，且 `validate_checkpoint_binding` 会在 resume 前 fail-closed，因此由领导保留为 non-blocking P2 backlog，不再为其返修。
- 固定上游 patch 与 apply 脚本在 `dc63735..1d74a1e` 零变化，SHA256 分别为 `A8425A7E8907089A01D7698C9E8D144E068417D98AED1D58DB8164279D1352B2`、`3D29ECFB96051EE3703B68E1BED1083555F3AB6B61B0EED99629886BF810E828`，pin 仍为 `2ffad63b25ddd79bfe25d3e046645401201c89d6`；`dc63735` 的真实 apply/diff/reverse/compile 门禁按批准规则标为 `carried-forward-unchanged`。
- 批次 2 放行只关闭本地 CPU 正式契约与报警能力；不代表 GPU-ready、BF16/FlashAttention/NCCL 实际执行、训练已复跑、Task 7、正式 10-task 或论文级复现放行。批次 2 详细证据与最终状态以 `docs/remediation/batch2/{PROGRESS,BLOCKED,decisions}.md` 为准。
- 批次 3 提交链为 `680d3d1302845b5052edcab002e809f35fada7b4`（实现）和 `687449031a92562c5fd2db93fbfb0283bba9aaae`（交付记录）；独立 CPU 验收在精确 `6874490` 技术通过：10/113 tests、静态与 formal dry-run 门禁、128-node/330-cell DAG、frontier/resume、六份 order metrics/两份 summary/60 hashes 及手算 population std 均符合判据，前后 checkout clean。
- 三项未预告抽查在 lineage、run/profile/contract binding 和 full-test count 处 fail-closed；`blocking` 为 none，未发现新 P2，既有 batch-2 string `require_ctan` P2 未重验。固定 patch 五项输入在 `1d74a1e..6874490` 零变化，继续按规则标为 `carried-forward-unchanged`。
- 该结论只覆盖本地 CPU/no-GPU orchestration 与 artifact/恢复/统计报警能力，不代表领导已放行、GPU-ready、训练/推理完成、Task 7、正式 10-task 或论文级复现通过；详细证据以 `docs/remediation/batch3/{PROGRESS,BLOCKED,decisions}.md` 为准。
- 各轮验收 checkout、缓存和负向探针均位于仓库外系统临时目录，只是临时输入，不是永久 artifact；版本化记录只保留判据、结果和非声明边界，不依赖这些路径长期存在。
- 角色例外记录：批次 1 正式验收、批次 2 首轮验收、`dc63735` 返修复验、`1d74a1e` 最终返修复验和 `6874490` 批次 3 主整改验收在裁决与仓库零变化复核完成后，领导均明确调用 `neat-freak` 做知识收口；批次 2 最终放行后又在独立咨询对话授权同一范围的收口。例外只允许同步 docs/rules 与只读清点残留，不改业务代码、测试、配置、正式 finding 或生成记忆，不删除现场，也不改变先前独立验收结论。
- 下一动作：领导决定是否最终放行批次 3 的本地 CPU/no-GPU 范围；在领导明确放行前批次 3 不关闭。若放行，后续仍须由独立咨询对话起草受限的 BF16/GPU gate 候选目标并经领导批准；在新目标获批前不得进入 GPU、Task 7、BF16 gate 或正式 10-task。

## 交付最小格式

- 整改交付必须同时给出：commit、实际测试输出、反向验证红→绿证据、白名单外 diff、BLOCKED、遗留风险和下一放行点。
- 验收交付必须给出：待验收 commit、现场环境、明卷结果、现场重跑与 `carried-forward-unchanged` 门禁、未预告抽查及最小复现、标准路径影响、`blocking`/`non-blocking P2`/`out-of-scope`、白名单核对、最终通过/不通过和遗留风险。
- 验收 Codex最终用五行内汇报“过没过／完成什么／遗留什么／下一步”；不能把 `merged`、`deployed`、`live verified` 或 `knowledge closed` 混为一谈。
