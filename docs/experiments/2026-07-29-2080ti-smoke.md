# RTX 2080 Ti reduced smoke test (2026-07-29)

## Scope and status

Status: **passed** for the reduced GRPO training path on the available
8 x RTX 2080 Ti node.

This is a hardware-compatibility and training-plumbing result. It is not a
paper reproduction result: RaPO was disabled, and the prompt, completion, and
image budgets were reduced. The gates progressed from two to the paper rollout
count of eight.

## Provenance

- RaPO repository commit for the successful one-step run: `f005605`
- RaPO repository commit for the two-rollout five-step run: `d822934`
- RaPO repository commit for the stable eight-rollout run: `9c03e7c`
- Visual-RFT upstream commit: `2ffad63b25ddd79bfe25d3e046645401201c89d6`
- Model: Qwen2-VL-2B-Instruct, 2,208,985,600 parameters
- Dataset: ImageNet-R order 0, Visual-RFT task 1 export
- Training environment: `rapo-train`, PyTorch 2.5.1, DeepSpeed 0.15.4
- Hardware: 8 x NVIDIA GeForce RTX 2080 Ti, compute capability 7.5

The reduced configuration used FP16, PyTorch SDPA, ZeRO-3, CPU optimizer
offload, gradient accumulation 2, gradient checkpointing, 512 prompt tokens, 32
completion tokens, and an image budget of 100,352 pixels. The first gates used
two generations and a dynamic FP16 scale of 4,096; the final gates used eight
generations and a scale of 2,048.

## Results

### Model-load gates

The frozen base model loaded on one RTX 2080 Ti in 11.12 seconds. Peak PyTorch
memory was 4,215 MiB allocated and 4,528 MiB reserved.

The final model from the two-rollout five-step run also loaded successfully on
one RTX 2080 Ti in 45.71 seconds. The final eight-rollout model loaded in 47.39
seconds. Both reported the same parameter count, 4,215 MiB peak allocated
memory, and 4,528 MiB peak reserved memory.

### FP16 loss-scale diagnosis

Three initial one-step trials started dynamic loss scaling at 65,536. All
completed without OOM, but reported a zero gradient norm despite nonzero
grouped reward variance. Inspection of the saved DeepSpeed optimizer state
showed `overflow=True` and an empty Adam state, proving that the optimizer step
had been skipped. Disabling gradient checkpointing did not change this result.

Reducing `initial_scale_power` from 16 to 12 fixed the skipped update. The
successful one-step run reported:

- `grad_norm=8.20528507232666`
- `reward_std=0.2651650309562683`
- DeepSpeed optimizer `overflow=False`
- Adam `step=1`, with nonempty `exp_avg` and `exp_avg_sq` tensors
- no OOM and no residual GPU process

An `nvidia-smi` snapshot near the high-memory phase showed 10,102--10,398 MiB
used per GPU. This is an observed snapshot, not an instrumented peak.

### Two-rollout five-step stability gate

The five-step run used all eight GPUs and `RAPO_SMOKE_SAVE_STRATEGY=no`.
This avoided writing the roughly 26 GiB distributed optimizer state while
retaining the final model.

| Step | Loss | Gradient norm | Reward | Reward std | KL |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 0.0000 | 8.2082 | 0.25000 | 0.26517 | 0.00000 |
| 2 | 0.0048 | 10.1267 | 0.34375 | 0.13258 | 0.11887 |
| 3 | 0.0048 | 16.5121 | 0.46875 | 0.22097 | 0.12073 |
| 4 | 0.0082 | 14.3893 | 0.81250 | 0.17678 | 0.20459 |
| 5 | 0.0069 | 12.0072 | 0.75000 | 0.17678 | 0.17255 |

Training took 141.17 seconds, or 0.035 optimizer steps per second. Every
gradient norm was finite and nonzero; no FP16 overflow, NaN, or OOM was
observed. DeepSpeed reported allocator cache flushes after later steps, so this
configuration is stable for the bounded gate but has limited memory headroom.

### Eight-rollout stability gate

An eight-rollout one-step run succeeded at loss scale 4,096 with
`grad_norm=16.1259` and `reward_std=0.48152`. A subsequent five-step run,
however, repeated both the previous gradient norm and learning rate at steps 3
and 5. This proves those two optimizer updates were skipped even though the
overall process exited successfully.

The eight-rollout five-step run was repeated from the base model with
`initial_scale_power=11`, or 2,048:

| Step | Loss | Gradient norm | Learning rate | Reward | Reward std | KL |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 0.0000 | 16.0320 | 8e-7 | 0.29688 | 0.48152 | 0.00000 |
| 2 | 0.0015 | 28.3033 | 6e-7 | 0.79688 | 0.91236 | 0.03763 |
| 3 | 0.0030 | 24.1122 | 4e-7 | 1.10938 | 0.85896 | 0.07617 |
| 4 | 0.0054 | 14.0488 | 2e-7 | 1.68750 | 0.39689 | 0.13562 |
| 5 | 0.0191 | 28.5391 | 0 | 1.70312 | 0.41113 | 0.47705 |

All five learning-rate transitions occurred, all gradient norms were finite
and distinct from the previous step, and no overflow, NaN, or OOM was
observed. Training took 201.39 seconds, or 0.025 optimizer steps per second,
with mean loss 0.00581. Observed GPU memory reached 10,806 MiB on the most
loaded rank, leaving very little headroom.

## Remote artifacts

Full logs and large model/checkpoint artifacts remain outside Git:

- successful auditable one-step output:
  `/home/zhenglifeng/outputs/rapo-smoke/2080ti-grpo-task01-step1-scale12`
- stable eight-rollout final model:
  `/home/zhenglifeng/outputs/rapo-smoke/2080ti-grpo-task01-step5-n8-scale11-nosave`
- logs:
  `/home/zhenglifeng/logs/rapo-smoke/`

Failed and superseded model/checkpoint directories are removed after their
diagnosis is recorded; their small text logs are retained.

## Decision

The available node is sufficient for reduced GRPO/RaPO integration work, so a
newer GPU is not yet required. The subsequent two-task execution and evaluation
gate is recorded in
[2026-07-29-2080ti-two-task.md](2026-07-29-2080ti-two-task.md). A newer card
should be requested before increasing the sequence or image budgets materially:
the current observed high-water mark leaves less than 0.5 GiB on the most
loaded rank.
